<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="ImageGlass.UI.IgWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cm="using:ImageGlass.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ImageGlass.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:DefaultBindMode="OneWay"
    mc:Ignorable="d">

    <Grid x:Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <!--  Titlebar  -->
        <local:TitlebarControl
            x:Name="PART_Titlebar"
            Grid.Row="0"
            Padding="{x:Bind TitleBarPadding}"
            Canvas.ZIndex="100"
            IsTabStop="False"
            Text="{x:Bind WindowTitle}"
            TextColor="{x:Bind cm:AP.Config.Theme.ComputedColors.TextColor}" />


        <!--  Window Content  -->
        <ContentPresenter
            x:Name="PART_WindowContent"
            Grid.Row="1"
            Content="{x:Bind WindowContent}"
            IsTabStop="False" />
    </Grid>

</Window>
